tasks.register('buildAll') {
    group = 'build'
    description = 'Build all LanguagePlugin Minecraft version variants.'
    dependsOn subprojects.collect { it.tasks.named('build') }
}

tasks.register('cleanAll') {
    group = 'build'
    description = 'Clean all LanguagePlugin Minecraft version variants.'
    dependsOn subprojects.collect { it.tasks.named('clean') }
}
